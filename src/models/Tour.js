export default class Tour {
  constructor(id, title, price, img, desc, schedule, group) {
    this.id = id;
    this.title = title;
    this.price = price;
    this.img = img;
    this.desc = desc;
    this.schedule = schedule;
    this.group = group;
  }
}
